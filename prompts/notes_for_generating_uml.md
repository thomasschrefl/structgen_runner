
Generate a PlantUML ACTIVITY DIAGRAM using the NEW activity diagram syntax.
Rules:
- Output ONLY PlantUML between @startuml and @enduml.
- Use: start, stop, :Action; lines.
- For decisions use: if (...) then (...) ... else (...) ... endif
- Keep it UML-ish: put guards in parentheses after then/else labels where appropriate.
